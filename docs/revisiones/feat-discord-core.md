# Revisión: feat/discord-core

Revisado: 11 archivos (6 producción en `src/main/java/com/discordtowny/discord/`, 5 pruebas en `src/test/java/com/discordtowny/discord/`), 2.302 líneas (+2.291 diff)
Build: Compilación exitosa, 27 tests en verde

## Hallazgos

| # | Severidad | Archivo:línea | Problema | Corrección propuesta |
|---|---|---|---|---|
| 1 | bloqueante | `src/main/java/com/discordtowny/discord/JdaGuildOperationExecutor.java:103` | En `createSpace` (y en `restoreSpace`, línea 249), nunca se asigna el rol de alcalde (`config.roles().mayorRoleName()`) a `req.mayorDiscordId()`, a pesar de viajar en `SpaceRequest`. Se viola `docs/spec.md` 4.1 (*"Al crear, el bot asigna el rol de town a todos los residentes ya vinculados y el rol de alcalde a quien ejecutó el comando"*). El alcalde nunca recibe su rol al crear el espacio. | En `createSpace` y `restoreSpace`, asegurar la existencia del rol de alcalde (`ensureRoleByName` o similar) y asignarlo explícitamente al miembro correspondiente a `req.mayorDiscordId()`. |
| 2 | bloqueante | `src/main/java/com/discordtowny/discord/JdaGuildOperationExecutor.java:306-310` | `guild.getMemberById(id)` consulta exclusivamente la caché en memoria de JDA. Al no haberse configurado precarga completa (`ChunkingFilter.ALL` / `MemberCachePolicy.ALL`) en `JdaDiscordGateway`, devuelve `null` para miembros no cacheados. En `applyMemberRoles`, si `member == null`, retorna `OperationOutcome.success()` asumiendo erróneamente que el usuario abandonó el guild, sin asignar ni revocar roles. Lo mismo afecta a `assignRolesToMembers` (línea 484). | Usar `guild.retrieveMemberById(id).complete()` cuando `guild.getMemberById(id)` devuelva `null` (o habilitar chunking de miembros en la configuración de JDA), manejando adecuadamente el error 404 si el usuario realmente no pertenece al servidor. |
| 3 | bloqueante | `src/main/java/com/discordtowny/discord/GuildOperationQueue.java:84-97` | `submit(GuildOperation operation)` no verifica si la cola está detenida (`running.get() == false`). Si se invoca `submit` después de `shutdown()` (o durante una carrera en el apagado), la tarea se añade a `queue`, pero el hilo consumidor ya murió, dejando el `CompletableFuture` colgado indefinidamente. | Comprobar `if (!running.get())` al inicio de `submit()` y retornar inmediatamente `CompletableFuture.completedFuture(OperationOutcome.transientFailure("Cola detenida"))`. |
| 4 | importante | `src/main/java/com/discordtowny/discord/JdaGuildOperationExecutor.java:60-68` | JDA detecta la falta de permisos y violaciones de jerarquía de forma local antes de la llamada HTTP, lanzando `InsufficientPermissionException` o `HierarchyException` (subclases de `RuntimeException`, no de `ErrorResponseException`). El bloque `catch (Exception e)` las clasifica como `TRANSIENT_FAILURE` en lugar de `PERMANENT_FAILURE`, disparando 5 reintentos con esperas de más de un minuto ante fallos permanentes. | Capturar explícitamente `net.dv8tion.jda.api.exceptions.PermissionException` (o sus subclases) antes de `catch (Exception e)` y retornar `OperationOutcome.permanentFailure(...)`. |
| 5 | importante | `src/main/java/com/discordtowny/discord/JdaGuildOperationExecutor.java:228-257` | En `restoreSpace`, la recreación del rol (`ensureRole`) no se persiste de inmediato en `SpaceRepository`. `spaces.save(...)` solo se llama al final del método. Si la reubicación de canales falla, el nuevo rol queda creado en Discord pero no en la base de datos (dejando el espacio como `ARCHIVED` con `roleId` vacío). | Persistir `space = withRole(space, role.getId())` mediante `spaces.save(space)` inmediatamente después de obtener el rol, manteniendo el patrón de persistencia paso a paso de `createSpace`. |
| 6 | importante | `src/main/java/com/discordtowny/discord/JdaGuildOperationExecutor.java:514` | Ni la cola ni el ejecutor marcan el espacio con estado `SpaceState.INCONSISTENT` ante fallos a medias o agotamiento de reintentos. Además, `newEmptySpace` inicializa el espacio directamente en `ACTIVE`, por lo que un fallo tras crear solo la categoría o el rol deja el registro en la BD marcado como activo en vez de inconsistente (`docs/spec.md` 10 y `ARCHITECTURE.md` 4). | Registrar el estado como `INCONSISTENT` cuando una operación falle de forma definitiva o agote sus reintentos, y considerar inicializar las creaciones en curso como inconsistentes hasta su finalización exitosa. |
| 7 | importante | `src/main/java/com/discordtowny/discord/JdaGuildOperationExecutor.java:186-188` | En `archiveSpace`, se deniega `Permission.VIEW_CHANNEL` a `@everyone` tras haber eliminado el rol de la town (`role.delete()`). El comentario dice `// Solo lectura: @everyone sin escribir`, pero al denegar `VIEW_CHANNEL` el canal se vuelve completamente invisible para todos, contradiciendo el criterio 10 de la spec (*"deja su canal archivado y legible"*). | Si los residentes deben poder leer el historial archivado, no se puede eliminar el rol sin reconfigurar permisos o debe permitirse `VIEW_CHANNEL` denegando únicamente el envío de mensajes. |
| 8 | importante | `src/main/java/com/discordtowny/discord/JdaDiscordGateway.java:82` | `connect()` llama a `awaitReady()` bloqueando el hilo invocador. Si se invoca desde `onEnable()` en el hilo principal del servidor, congela el arranque de Paper ante lentitud de red o caída de Discord, violando el principio P4 ("El hilo principal es sagrado"). Asimismo, `verifyPermissions()` (línea 167) ejecuta `spaces.findAll()`, realizando I/O bloqueante de base de datos. | Conectar de forma asíncrona fuera del hilo principal y asegurar que las comprobaciones de base de datos no se invoquen desde el hilo principal. |
| 9 | importante | `src/main/java/com/discordtowny/discord/LogQueue.java:171` | En `LogQueue`, `e.getMessage()` se envía al logger sin pasar por `sanitizeMessage`. En `GuildOperationQueue` (líneas 118 y 147), `logger.log(Level.SEVERE, ..., t)` y `logger.warning(..., t.getMessage())` imprimen la traza completa y mensajes sin sanitizar. Si una excepción incluye el token o credenciales, se filtran al log del servidor (principio P7). | Pasar todos los mensajes de excepción y trazas por una función de sanitización que oculte el token antes de escribirlos al logger. |
| 10 | menor | `src/main/java/com/discordtowny/discord/JdaGuildOperationExecutor.java:342` | `isManagedRole` ejecuta `spaces.findAll()` en cada llamada dentro de los bucles de concesión y revocación de `applyMemberRoles`, provocando múltiples lecturas completas a base de datos en una sola operación. | Obtener el conjunto de IDs de roles gestionados una sola vez antes de iterar sobre `grantRoleIds` y `revokeRoleIds`. |
| 11 | menor | `src/main/java/com/discordtowny/discord/JdaGuildOperationExecutor.java:493-505` | En `classifyError`, errores definitivos como `UNKNOWN_CHANNEL` o `UNKNOWN_ROLE` (códigos 10003/10011) caen en `default` (`TRANSIENT_FAILURE`), provocando que `deleteSpace` reintente 5 veces si un administrador borró el canal a mano en Discord. | Clasificar `UNKNOWN_CHANNEL` y `UNKNOWN_ROLE` como operaciones idempotentes exitosas durante borrados, o como fallos permanentes en el resto. |
| 12 | menor | `src/test/java/com/discordtowny/discord/GuildOperationQueueTest.java:87-135` | El test `retryDoesNotDuplicate` afirma verificar que un reintento tras fallo parcial no duplica recursos, pero utiliza un ejecutor mock donde la idempotencia está simulada manualmente por el propio test (`if (!createdRoles.contains(...))`). No verifica la idempotencia real del código de producción. | Renombrar el test para reflejar que solo comprueba la lógica de reintento de la cola, y añadir pruebas sobre `JdaGuildOperationExecutor` que verifiquen la no duplicación real de canales y roles existentes. |
| 13 | menor | `src/main/java/com/discordtowny/discord/JdaGuildOperationExecutor.java:462-479` | `applyChannelPermissions` y `applyVoiceChannelPermissions` (usados al restaurar espacio) no configuran override explícito para el bot (`guild.getSelfMember()`), a diferencia de `ensureTextChannel` y `ensureVoiceChannel`. | Incluir el override del bot con `VIEW_CHANNEL`, `MANAGE_CHANNEL` y `MESSAGE_SEND` / `VOICE_CONNECT` al restaurar permisos. |

Veredicto: requiere correcciones

## Cierre

Revisado en commit `abd19c3`.

| # | Estado | Comprobación |
|---|---|---|
| 1 | resuelto | Se implementó `ensureMayorRole()` y en `createSpace` (L129-133) y `restoreSpace` (L302-306) se busca/crea el rol de alcalde y se asigna junto al rol de town a `req.mayorDiscordId()` de forma idempotente (`assignRoleToMember`). |
| 2 | resuelto | Se configuró `MemberCachePolicy.ALL` y `ChunkingFilter.ALL` en el gateway; `findMember` (L632-653) llama a `retrieveMemberById().complete()` en hilo virtual si no está en caché, distinguiendo 404 (`UNKNOWN_MEMBER`/`UNKNOWN_USER`) de fallos transitorios que se relanzan a la cola. |
| 3 | resuelto | En `GuildOperationQueue.submit` (L103-121) se comprueba `!running.get()` al inicio y tras `offer()` ante carreras con `shutdown()`, completando de inmediato con fallo transitorio ("Cola detenida"). |
| 4 | resuelto | Se capturó explícitamente `PermissionException` (L66-73) antes de `catch (Exception e)`, clasificándolo como `PERMANENT_FAILURE` y notificando `onOperationFailed`. |
| 5 | resuelto | En `restoreSpace` (L278-282), se ejecuta `spaces.save(space)` con `withRole` inmediatamente después de crear/obtener el rol, antes de mover canales. |
| 6 | resuelto | `newEmptySpace` nace en `INCONSISTENT` (L738-744) y solo pasa a `ACTIVE` al completar todo con éxito (L136-137); `restoreSpace` marca `INCONSISTENT` al entrar (L268-269); y `onOperationFailed` (L700-734) actualiza la BD a `INCONSISTENT` ante fallos permanentes o reintentos agotados. |
| 7 | resuelto | Acorde a la clarificación de spec (`spec.md` 4.3 y 10), `archiveSpace` (L220-243) deniega `VIEW_CHANNEL` a `@everyone` ocultándolo a ex-residentes tras borrar el rol, y otorga override al bot; el staff mantiene visibilidad y lectura mediante permisos nativos de administrador. |
| 8 | resuelto | `connect()` (L67-70) ahora es asíncrono devolviendo `CompletableFuture<Void>` en hilo virtual (`dt-discord-connect`) donde se evalúa `verifyPermissions()`; y se añadió `verifyPermissionsAsync()` a la interfaz. |
| 9 | resuelto | Se creó `DiscordSanitizer` (con soporte para texto y trazas completas) e integró en `LogQueue` (L175-178), `GuildOperationQueue` (L139-178) y `JdaGuildOperationExecutor` (L81-85), enmascarando cualquier token con `[TOKEN_OCULTO]`. |
| 10 | resuelto | En `applyMemberRoles` (L388-390), se invoca `spaces.findAll()` una sola vez para construir `managedRoleIds` en memoria antes de evaluar las listas de concesión y revocación. |
| 11 | resuelto | En `deleteSpace` (L330-369) se capturan e ignoran `UNKNOWN_ROLE` y `UNKNOWN_CHANNEL` como éxito idempotente, y en `classifyError` (L680-686) se mapean a `success()` durante `DeleteSpace` y a `permanentFailure` para las demás operaciones. |
| 12 | resuelto | Se renombró el test en la cola a `retryOnTransientFailureCompletesSuccessfully` y se añadieron pruebas reales en `JdaGuildOperationExecutorTest` (L188-360) que verifican la no duplicación de categorías, roles y canales en el código de producción. |
| 13 | resuelto | Se añadió override explícito para el bot (`guild.getSelfMember()`) con permisos de visualización y gestión en `applyChannelPermissions` (L600-605) y `applyVoiceChannelPermissions` (L620-625). |

### Comprobación de cambios de especificación

- **Categorías numeradas al llegar a 50 canales:** `ensureCategoryWithCapacity` calcula `cat.getChannels().size() + channelsNeeded <= 50`, teniendo en cuenta los canales a crear en la operación para no superar el límite. Si una categoría numerada ya existe pero también está llena, el bucle incrementa `index` hasta encontrar una con capacidad suficiente o instanciar la siguiente libre en secuencia.
- **Canal archivado visible solo para administradores:** Al eliminar el rol de la town y denegar `VIEW_CHANNEL` a `@everyone`, los ex-residentes no tienen acceso al canal. Los administradores del servidor lo leen gracias al bypass nativo de `ADMINISTRATOR` en Discord, preservando el cupo de roles del servidor.

Veredicto final: integrable
