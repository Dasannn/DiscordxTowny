# Revisión: feat/vinculacion

Revisado: 12 archivos, 1727 líneas: los cinco archivos de `src/main/java/com/discordtowny/link/`, `LinkMinecraftCommands.java`, `LinkSlashCommands.java` y sus cinco archivos de pruebas y soporte. Contrastados con los cinco documentos rectores y las dependencias de almacenamiento, Towny y Discord. Referencias de producción relativas a `src/main/java/com/discordtowny/`; referencias de pruebas relativas a `src/test/java/com/discordtowny/`.
Build: verde, 209 tests

## Hallazgos

| # | Severidad | Archivo:línea | Problema | Corrección propuesta |
|---|---|---|---|---|
| 1 | bloqueante | `link/DefaultLinkService.java:152`, `:213`, `:231` | Leer el código, insertar el vínculo y borrar el código son operaciones independientes. Si falla el borrado después de insertar, queda un código utilizado todavía canjeable después de desvincular. También pueden dos canjes leer el mismo código; el primero termina, se desvincula y el segundo inserta desde su lectura antigua: ambos obtienen éxito. Las restricciones UNIQUE solo protegen los vínculos que existen en ese instante. La regeneración tampoco invalida una lectura de código que ya está en curso. | Coordinar con almacenamiento una operación transaccional que compruebe vigencia y consuma el código junto con la inserción del vínculo, excluyendo su sustitución concurrente. Añadir pruebas con barreras para canje/regeneración, canje/desvinculación y fallo al borrar el código; comprobar que nunca se acepta dos veces. |
| 2 | bloqueante | `link/DefaultLinkService.java:276`, `:315`, `:320` | Se borra el vínculo antes de retirar los roles. Si falla `spaceRepository.findAll()`, el error se ignora y se envía una revocación vacía. Además, se ignora el `OperationOutcome` de Discord: con el bot desconectado el gateway devuelve inmediatamente un fallo, pero `unlink` confirma éxito. El usuario conserva acceso sin vínculo y repetir la desvinculación devuelve `false`, sin reintentar la retirada. | No convertir fallos de almacenamiento o Discord en éxito. Conservar de forma recuperable el Discord ID y la revocación pendiente hasta confirmar la retirada; encadenar y comprobar el resultado de la cola. Coordinar los cambios de persistencia necesarios y probar ambos fallos. |
| 3 | bloqueante | `link/DefaultLinkService.java:309` | La lista de retirada solo contiene `space.roleId()`. Nunca incluye el rol global de alcalde; `ApplyMemberRoles` elimina exclusivamente los IDs recibidos. Un alcalde desvinculado conserva una distinción y llave de comandos que exige identidad verificada. | Obtener también el ID del rol de alcalde gestionado y retirarlo junto con todos los roles de town. Coordinar el contrato necesario con Discord y probar una cuenta que tenga ambos tipos de rol. |
| 4 | bloqueante | `link/DefaultLinkService.java:142`, `:154`, `:234`; `link/AttemptTracker.java:23` | El límite puede eludirse. Comprobar bloqueo y registrar fallo no es atómico: varias solicitudes pasan la comprobación antes de registrar sus fallos y todas prueban códigos. Además, con el valor por defecto de 5, un atacante puede fallar cuatro veces, vincular una cuenta Minecraft propia, desvincularla y repetir: el éxito borra su contador sin esperar los 15 minutos. | Mantener el presupuesto de fallos durante una ventana temporal independientemente de vincular/desvincular y serializar la admisión y resolución de intentos por Discord ID. Probar ráfagas concurrentes y el ciclo de cuatro fallos, canje propio y desvinculación. |
| 5 | bloqueante | `link/DefaultLinkService.java:198`, `:332`, `:343` | Ambas consultas a Towny se ejecutan dentro de `supplyAsync`. `LiveTownyFacade` rechaza ese hilo; los `catch` ocultan el error. El camino alternativo sin `SyncService` devuelve `SUCCESS` sin sincronizar ningún rol y tampoco contempla el rol de alcalde. La consulta del nombre falla igualmente si no está en memoria, por ejemplo tras un reinicio. | Leer Towny mediante el scheduler del hilo principal y pasar snapshots al pool. Delegar la sincronización en el contrato previsto, sin una implementación alternativa incompleta ni excepciones silenciadas; hacer observable cualquier fallo del futuro de sincronización. |
| 6 | importante | `discord/LinkSlashCommands.java:60` | La respuesta de `/link` puede ser pública si `ephemeral` está desactivado. Contradice la obligación específica de la sección 2.1 y el propio comentario del método. El test solo configura `true`, por lo que no verifica que la privacidad sea obligatoria. | Forzar `deferReply(true)` para vinculación y probar también una configuración con `ephemeral=false`. |
| 7 | importante | `link/DefaultLinkService.java:159`, `:175` | Se envía el texto íntegro del código introducido al canal de auditoría mediante `AuditEvent.target`; el gateway publica ese campo. Estos caminos corresponden a códigos inexistentes o caducados, por lo que no demuestran por sí solos una fuga de un código vivo, pero sí almacenan y publican material de vinculación y posibles erratas de un secreto. | Omitir el código de todos los eventos; conservar actor, acción y motivo. Añadir una comprobación de que ningún campo del evento contiene el texto recibido. |
| 8 | importante | `minecraft/LinkMinecraftCommands.java:59`, `:89`, `:128` | Las continuaciones y manejadores de error llaman a `sendMessage` directamente desde el hilo que completa los futuros, normalmente el pool. Falta el retorno al scheduler exigido por la sección 3 de la arquitectura para hablar con jugadores. | Programar en el hilo principal todas las respuestas posteriores al trabajo asíncrono, incluidos los errores; probar con un futuro que se complete desde otro hilo. |
| 9 | importante | `discord/LinkSlashCommands.java:108`; `link/DefaultLinkService.java:268`, `:276` | `/unlink` autoriza usando un vínculo leído y después elimina únicamente por UUID. Si entre ambas operaciones se desvincula y se crea un vínculo nuevo para ese UUID, una solicitud antigua de la cuenta Discord anterior puede borrar el vínculo nuevo. En el servicio existe otra separación entre leer el vínculo y borrarlo, que puede además revocar los roles de una identidad distinta de la eliminada. | Hacer la desvinculación condicional al vínculo esperado, incluyendo Discord ID y una versión o fecha que distinga recreaciones; devolver el vínculo efectivamente eliminado para revocar sus roles. Coordinar el contrato y probar el cambio de vínculo entre lectura y borrado. |
| 10 | importante | `discord/LinkSlashCommands.java:39`, `:49` | Se anuncian y ejecutan siempre ambos comandos sin consultar `DiscordCommand.enabled`; tampoco se aplica `config.commands().cooldown()`. La configuración documentada en las secciones 5 y 5.1 no tiene efecto en estos adaptadores. | Filtrar el registro y rechazar la ejecución de comandos desactivados; aplicar el cooldown por usuario antes de delegar. Probar que en ambos casos no se invoca el servicio. |
| 11 | importante | `link/DefaultLinkService.java:354`; dependencia `storage/SqlLinkRepository.java:87` | La clasificación de unicidad confía en el texto «Ya existe», pero el repositorio lo asigna a cualquier `SQLIntegrityConstraintViolationException`, no solo a duplicados. Por tanto, la distinción todavía no es genuina para MySQL/MariaDB: un fallo de otra restricción puede entrar en este camino, borrar el código y, si aparece un vínculo concurrente, convertirse en un resultado normal. El test añadido solo simula un error genérico de I/O. | Coordinar con T2 la clasificación por códigos específicos de duplicado, conservando los demás errores como fallos; no decidir por un mensaje genérico. Probar una excepción de integridad que no sea duplicado, incluida la aparición concurrente de un vínculo, y verificar que no se borra el código. |
| 12 | importante | Pruebas: `link/DefaultLinkServiceTest.java:275`, `:325`, `:416` | Varias comprobaciones no acreditan lo anunciado: `exitoLimpiaContadorDeFallos` solo comprueba que existe el vínculo; `unlinkEliminaVinculoYRetiraRoles` no crea roles ni inspecciona la lista de retirada, por lo que pasa con una orden vacía; `choqueDeUnicidadPorConcurrenciaSeDetectaCorrectamente` prefabrica la excepción y ejecuta un único canje. Tampoco el ciclo de persistencia reabre el almacenamiento para verificar el reinicio. | Sustituir esas comprobaciones por efectos observables: presupuesto de intentos conforme a la política corregida, IDs exactos retirados y resultado fallido de Discord, peticiones realmente concurrentes con barreras y reapertura de la base de datos. Mantener el test de excepción como prueba de traducción de errores, sin atribuirle cobertura de concurrencia. |
| 13 | menor | `link/CodeGenerator.java:20`; `link/AttemptTracker.java:16` | Riesgo residual de fuerza bruta distribuida, no incumplimiento del límite por usuario especificado: el alfabeto real tiene 31 símbolos, luego hay 31^6 = 887.503.681 códigos, unos 29,73 bits. Cinco intentos distintos dan una probabilidad de 5/887.503.681 ≈ 5,63×10^-9 contra un código concreto. Mil cuentas elevan el presupuesto a 5000 intentos y la probabilidad a ≈ 5,63×10^-6; contra M códigos vivos crece aproximadamente por M mientras la probabilidad sea pequeña. No existe presupuesto agregado. Estos cálculos presuponen corregido el hallazgo 4. | Documentar el riesgo y someter al arquitecto una defensa agregada por ventana si se requiere resistencia a múltiples cuentas; no presentar el límite individual como protección global ni añadir requisitos sin aprobación. |
| 14 | menor | Pruebas: `link/CodeGeneratorTest.java:71` | La prueba exige que 1000 muestras aleatorias nunca colisionen. Con el espacio real, la probabilidad de una colisión en esa muestra es aproximadamente 0,0563 %; un generador correcto puede hacer fallar el build. La diversidad tampoco demuestra seguridad criptográfica. | Eliminar la exigencia probabilística de unicidad absoluta; verificar determinísticamente el uso del generador inyectado y la gestión de colisiones en persistencia. |

Veredicto: requiere correcciones
