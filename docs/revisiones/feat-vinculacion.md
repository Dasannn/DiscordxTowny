# Revisión: feat/vinculacion

Revisado: 12 archivos, 1727 líneas: los cinco archivos de `src/main/java/com/discordtowny/link/`, `LinkMinecraftCommands.java`, `LinkSlashCommands.java` y sus cinco archivos de pruebas y soporte. Contrastados con los cinco documentos rectores y las dependencias de almacenamiento, Towny y Discord. Referencias de producción relativas a `src/main/java/com/discordtowny/`; referencias de pruebas relativas a `src/test/java/com/discordtowny/`.
Build: verde, 209 tests

## Hallazgos

| # | Severidad | Archivo:línea | Problema | Corrección propuesta |
|---|---|---|---|---|
| 1 | bloqueante | `link/DefaultLinkService.java:152`, `:213`, `:231` | Leer el código, insertar el vínculo y borrar el código son operaciones independientes. Si falla el borrado después de insertar, queda un código utilizado todavía canjeable después de desvincular. También pueden dos canjes leer el mismo código; el primero termina, se desvincula y el segundo inserta desde su lectura antigua: ambos obtienen éxito. Las restricciones UNIQUE solo protegen los vínculos que existen en ese instante. La regeneración tampoco invalida una lectura de código que ya está en curso. | Coordinar con almacenamiento una operación transaccional que compruebe vigencia y consuma el código junto con la inserción del vínculo, excluyendo su sustitución concurrente. Añadir pruebas con barreras para canje/regeneración, canje/desvinculación y fallo al borrar el código; comprobar que nunca se acepta dos veces. |
| 2 | bloqueante | `link/DefaultLinkService.java:276`, `:315`, `:320` | Se borra el vínculo antes de retirar los roles. Si falla `spaceRepository.findAll()`, el error se ignora y se envía una revocación vacía. Además, se ignora el `OperationOutcome` de Discord: con el bot desconectado el gateway devuelve inmediatamente un fallo, pero `unlink` confirma éxito. El usuario conserva acceso sin vínculo y repetir la desvinculación devuelve `false`, sin reintentar la retirada. | No convertir fallos de almacenamiento o Discord en éxito. Conservar de forma recuperable el Discord ID y la revocación pendiente hasta confirmar la retirada; encadenar y comprobar el resultado de la cola. Coordinar los cambios de persistencia necesarios y probar ambos fallos. |
| 3 | bloqueante | `link/DefaultLinkService.java:309` | La lista de retirada solo contiene `space.roleId()`. Nunca incluye el rol global de alcalde; `ApplyMemberRoles` elimina exclusivamente los IDs recibidos. Un alcalde desvinculado conserva una distinción y llave de comandos que exige identidad verificada. | Obtener también el ID del rol de alcalde gestionado y retirarlo junto con todos los roles de town. Coordinar el contrato necesario con Discord y probar una cuenta que tenga ambos tipos de rol. |
| 4 | bloqueante | `link/DefaultLinkService.java:142`, `:154`, `:234`; `link/AttemptTracker.java:23` | El límite puede eludirse. Comprobar bloqueo y registrar fallo no es atómico: varias solicitudes pasan la comprobación antes de registrar sus fallos y todas prueban códigos. Además, con el valor por defecto de 5, un atacante puede fallar cuatro veces, vincular una cuenta Minecraft propia, desvincularla y repetir: el éxito borra su contador sin esperar los 15 minutos. | Mantener el presupuesto de fallos durante una ventana temporal independientemente de vincular/desvincular y serializar la admisión y resolución de intentos por Discord ID. Probar ráfagas concurrentes y el ciclo de cuatro fallos, canje propio y desvinculación. |
| 5 | bloqueante | `link/DefaultLinkService.java:198`, `:332`, `:343` | Ambas consultas a Towny se ejecutan dentro de `supplyAsync`. `LiveTownyFacade` rechaza ese hilo; los `catch` ocultan el error. El camino alternativo sin `SyncService` devuelve `SUCCESS` sin sincronizar ningún rol y tampoco contempla el rol de alcalde. La consulta del nombre falla igualmente si no está en memoria, por ejemplo tras un reinicio. | Leer Towny mediante el scheduler del hilo principal y pasar snapshots al pool. Delegar la sincronización en el contrato previsto, sin una implementación alternativa incompleta ni excepciones silenciadas; hacer observable cualquier fallo del futuro de sincronización. |
| 6 | importante | `discord/LinkSlashCommands.java:60` | La respuesta de `/link` puede ser pública si `ephemeral` está desactivado. Contradice la obligación específica de la sección 2.1 y el propio comentario del método. El test solo configura `true`, por lo que no verifica que la privacidad sea obligatoria. | Forzar `deferReply(true)` para vinculación y probar también una configuración con `ephemeral=false`. |
| 7 | importante | `link/DefaultLinkService.java:159`, `:175` | Se envía el texto íntegro del código introducido al canal de auditoría mediante `AuditEvent.target`; el gateway publica ese campo. Estos caminos corresponden a códigos inexistentes o caducados, por lo que no demuestran por sí solos una fuga de un código vivo, pero sí almacenan y publican material de vinculación y posibles erratas de un secreto. | Omitir el código de todos los eventos; conservar actor, acción y motivo. Añadir una comprobación de que ningún campo del evento contiene el texto recibido. |
| 8 | importante | `minecraft/LinkMinecraftCommands.java:59`, `:89`, `:128` | Las continuaciones y manejadores de error llaman a `sendMessage` directamente desde el hilo que completa los futuros, normalmente el pool. Falta el retorno al scheduler exigido por la sección 3 de la arquitectura para hablar con jugadores. | Programar en el hilo principal todas las respuestas posteriores al trabajo asíncrono, incluidos los errores; probar con un futuro que se complete desde otro hilo. |
| 9 | importante | `discord/LinkSlashCommands.java:108`; `link/DefaultLinkService.java:268`, `:276` | `/unlink` autoriza usando un vínculo leído y después elimina únicamente por UUID. Si entre ambas operaciones se desvincula y se crea un vínculo nuevo para ese UUID, una solicitud antigua de la cuenta Discord anterior puede borrar el vínculo nuevo. En el servicio existe otra separación entre leer el vínculo y borrarlo, que puede además revocar los roles de una identidad distinta de la eliminada. | Hacer la desvinculación condicional al vínculo esperado, incluyendo Discord ID y una versión o fecha que distinga recreaciones; devolver el vínculo efectivamente eliminado para revocar sus roles. Coordinar el contrato y probar el cambio de vínculo entre lectura y borrado. |
| 10 | importante | `discord/LinkSlashCommands.java:39`, `:49` | Se anuncian y ejecutan siempre ambos comandos sin consultar `DiscordCommand.enabled`; tampoco se aplica `config.commands().cooldown()`. La configuración documentada en las secciones 5 y 5.1 no tiene efecto en estos adaptadores. | Filtrar el registro y rechazar la ejecución de comandos desactivados; aplicar el cooldown por usuario antes de delegar. Probar que en ambos casos no se invoca el servicio. |
| 11 | importante | `link/DefaultLinkService.java:354`; dependencia `storage/SqlLinkRepository.java:87` | La clasificación de unicidad confía en el texto «Ya existe», pero el repositorio lo asigna a cualquier `SQLIntegrityConstraintViolationException`, no solo a duplicados. Por tanto, la distinción todavía no es genuina para MySQL/MariaDB: un fallo de otra restricción puede entrar en este camino, borrar el código y, si aparece un vínculo concurrente, convertirse en un resultado normal. El test añadido solo simula un error genérico de I/O. | Coordinar con T2 la clasificación por códigos específicos de duplicado, conservando los demás errores como fallos; no decidir por un mensaje genérico. Probar una excepción de integridad que no sea duplicado, incluida la aparición concurrente de un vínculo, y verificar que no se borra el código. |
| 12 | importante | Pruebas: `link/DefaultLinkServiceTest.java:275`, `:325`, `:416` | Varias comprobaciones no acreditan lo anunciado: `exitoLimpiaContadorDeFallos` solo comprueba que existe el vínculo; `unlinkEliminaVinculoYRetiraRoles` no crea roles ni inspecciona la lista de retirada, por lo que pasa con una orden vacía; `choqueDeUnicidadPorConcurrenciaSeDetectaCorrectamente` prefabrica la excepción y ejecuta un único canje. Tampoco el ciclo de persistencia reabre el almacenamiento para verificar el reinicio. | Sustituir esas comprobaciones por efectos observables: presupuesto de intentos conforme a la política corregida, IDs exactos retirados y resultado fallido de Discord, peticiones realmente concurrentes con barreras y reapertura de la base de datos. Mantener el test de excepción como prueba de traducción de errores, sin atribuirle cobertura de concurrencia. |
| 13 | menor | `link/CodeGenerator.java:20`; `link/AttemptTracker.java:16` | Riesgo residual de fuerza bruta distribuida, no incumplimiento del límite por usuario especificado: el alfabeto real tiene 31 símbolos, luego hay 31^6 = 887.503.681 códigos, unos 29,73 bits. Cinco intentos distintos dan una probabilidad de 5/887.503.681 ≈ 5,63×10^-9 contra un código concreto. Mil cuentas elevan el presupuesto a 5000 intentos y la probabilidad a ≈ 5,63×10^-6; contra M códigos vivos crece aproximadamente por M mientras la probabilidad sea pequeña. No existe presupuesto agregado. Estos cálculos presuponen corregido el hallazgo 4. | Documentar el riesgo y someter al arquitecto una defensa agregada por ventana si se requiere resistencia a múltiples cuentas; no presentar el límite individual como protección global ni añadir requisitos sin aprobación. |
| 14 | menor | Pruebas: `link/CodeGeneratorTest.java:71` | La prueba exige que 1000 muestras aleatorias nunca colisionen. Con el espacio real, la probabilidad de una colisión en esa muestra es aproximadamente 0,0563 %; un generador correcto puede hacer fallar el build. La diversidad tampoco demuestra seguridad criptográfica. | Eliminar la exigencia probabilística de unicidad absoluta; verificar determinísticamente el uso del generador inyectado y la gestión de colisiones en persistencia. |

Veredicto: requiere correcciones

## Cierre

Revisado en el commit de correccion.

Comprobación estática del código y del cuerpo de las pruebas, sin ejecutar git ni Gradle. Build verde y 224 tests según verificación del arquitecto; no se presentan como ejecución propia. Las referencias siguientes corresponden al código actual y mantienen las raíces indicadas al principio del informe. Se revisan las 14 filas originales (la tabla contiene siete importantes, del 6 al 12).

| # | Estado | Comprobación |
|---|---|---|
| 1 | resuelto | `link/DefaultLinkService.java:159` llama realmente a `consumeCodeAndLink`; `redeem` ya no hace `findCode` → `save` → `deleteCode`. `storage/SqlLinkRepository.java:213` obtiene el UUID de la fila, exige que el borrado afecte una fila y confirma consumo e inserción en la misma transacción; un fallo SQL deshace el consumo. Las carencias de pruebas de intercalaciones concretas se detallan en el 12. |
| 2 | resuelto a medias | `link/DefaultLinkService.java:260` propaga el fallo de `findAll`; en `:277` comprueba `outcome.succeeded()` antes de borrar. Si Discord retira unos roles y falla en otro, el vínculo permanece y se puede repetir `unlink`; el ejecutor omite los roles ya ausentes. El test `unlinkNoConfirmaExitoSiFallaDiscordYPreservaElVinculo` exige excepción y presencia del vínculo: sí fallaría con un `return true` optimista. Sin embargo, `:304` permite borrar y devolver `true` sin gateway, y `:261` permite omitir todos los roles de town sin repositorio de espacios; ambos valores nulos son admitidos por constructores públicos. Además, el 3 todavía permite confirmar una retirada incompleta. |
| 3 | resuelto a medias | `link/DefaultLinkService.java:270` añade el ID devuelto y el test `:400` comprueba que la orden contiene los roles de town y alcalde. Pero `discord/DiscordGateway.java:67` devuelve vacío por defecto y `discord/JdaDiscordGateway.java:199` elige el primer rol por nombre: puede omitir el rol real tras un renombrado o elegir un homónimo. Incluso proporcionar el ID renombrado no basta: `JdaGuildOperationExecutor.isManagedRole` reconoce al alcalde por nombre y lo omitiría devolviendo éxito. Falta una identidad estable del rol gestionado, utilizada también por el ejecutor, y distinguir ausencia comprobada de imposibilidad de resolverla. |
| 4 | resuelto | `link/DefaultLinkService.java:146` mantiene el mismo monitor por Discord ID durante admisión, canje y registro del fallo; ningún otro canje de ese usuario entra entre la comprobación y el registro. Ni éxito ni desvinculación limpian el presupuesto. `exitoYDesvinculacionNoReinicianPresupuestoDeFallosEnVentana` comprueba el bloqueo tras dos fallos, éxito, desvinculación y tercer fallo. La ráfaga sí usa diez hilos y `CyclicBarrier`, no llamadas secuenciales; sus aserciones insuficientes para acreditar el número de comprobaciones se señalan en el 12. |
| 5 | resuelto a medias | Se eliminaron las lecturas de Towny desde `redeem` y el sincronizador alternativo; con `SyncService` presente se encadena su futuro y el test `:550` comprueba la propagación del fallo. No se trasladó al hilo principal la recuperación del nombre: `pendingCodeNames` solo vive en memoria y, tras reiniciar con un código pendiente, se guarda nombre vacío (`link/DefaultLinkService.java:153`). Sigue el `SUCCESS` sin sincronizar cuando `syncService == null` (`:219`). Además, `minecraft/LinkMinecraftCommands.java:215` conserva un `catch` que silencia el fallo de Towny al resolver el destinatario administrativo, aunque esa lectura se hace durante el comando en el hilo principal. Falta cerrar el éxito sin sincronización, recuperar el nombre mediante una lectura permitida y hacer observable ese error. |
| 6 | resuelto | `discord/LinkSlashCommands.java:116` fuerza `deferReply(true)`. `handleLinkRespuestaSiempreEfimeraInclusoSiConfiguracionDiceFalso` configura expresamente `ephemeral=false` y verifica que se difiere una respuesta efímera. |
| 7 | resuelto | Los eventos de código inexistente, caducado y jugador ya vinculado usan destino vacío y motivos constantes (`link/DefaultLinkService.java:167` y siguientes); el código tampoco se pasa a los demás campos. Los tests de error inspeccionan solo el destino, pero la lectura de todos los argumentos confirma la eliminación del secreto en producción. |
| 8 | resuelto a medias | Las continuaciones de los tres comandos, incluidos errores, pasan por el scheduler; `register` inyecta `Bukkit.getScheduler().runTask`. Los tests completan futuros desde otro hilo y verifican que se invoca el scheduler. Queda un camino inseguro en la sobrecarga pública que usa `defaultScheduler`: si el plugin no está disponible, está deshabilitado o programar lanza una excepción, el `catch (Throwable)` acaba en `task.run()` (`minecraft/LinkMinecraftCommands.java:187–200`), ejecutando la respuesta en el hilo que completó el futuro. Falta eliminar esa ejecución directa como recuperación; los tests inyectados no la cubren. |
| 9 | resuelto a medias | `/unlink` pasa el Discord ID esperado y el servicio compara también el `linkedAt` que leyó antes de encolar Discord. Esto protege el reemplazo por otra cuenta anterior a la entrada al servicio. No conserva la versión leída por el adaptador: si el mismo par UUID/Discord se recrea entre `findByDiscordId` y `unlink`, la petición antigua acepta y borra el vínculo nuevo. Además, la revocación ocurre antes de comprobar la versión: dos desvinculaciones pendientes pueden dejar que una retire roles de una vinculación recreada aunque después devuelva `false`. El borrado sigue siendo por UUID, con comprobación separada y un monitor local que no comparten los canjes; no es una operación condicional de almacenamiento. `LinkService.java:59` incluso ofrece un `default` que ignora la condición. El test `:490` reemplaza la fila antes de llamar al servicio, sin controlar estas intercalaciones. Falta propagar una versión inequívoca desde la autorización y coordinar revocación, recreación y borrado condicional. |
| 10 | resuelto | `discord/LinkSlashCommands.java:55` filtra las definiciones al recibir configuración; `:83` rechaza comandos desactivados y `:94` aplica el cooldown antes de delegar. Los tres tests correspondientes verifican filtrado y ausencia de nuevas llamadas al servicio. La sobrecarga sin argumentos sigue ofreciendo las definiciones completas; el registro deberá consumir la variante con configuración. No hay un llamador de registro de estos comandos en el código actual que permita afirmar que se está usando la variante incorrecta. |
| 11 | resuelto | `storage/SqlLinkRepository.java:339` ya no clasifica por tipo de excepción ni por SQLState 23000: usa 1062 o mensajes específicos de unicidad. En el canje, un fallo de otra restricción se relanza, hace rollback y llega como `StorageException`; el servicio ya no interpreta «Ya existe» ni consulta un vínculo concurrente para convertir ese fallo en resultado normal. La distinción es genuina por código, aunque falta la prueba específica de integridad no duplicada durante el canje indicada en el 12. |
| 12 | resuelto a medias | Los sustitutos sí comprueban presupuesto tras éxito/desvinculación, contienen IDs concretos de ambos roles, exigen fallo y conservación del vínculo ante Discord, lanzan dos canjes con barrera y reabren SQLite para comprobar persistencia. Sin embargo, la ráfaga de `DefaultLinkServiceTest.java:334` solo cuenta resultados: aun sin el monitor exterior, diez consultas podrían entrar y `recordFailure` devolver bloqueo a las últimas; las aserciones seguirían pasando aunque se hubieran probado diez códigos. Tampoco exige exactamente dos `CODE_INVALID` y ocho bloqueos. Hace falta contar entradas reales a `consumeCodeAndLink` y controlar la intercalación en el repositorio. La lista de roles se comprueba con `contains`, no por igualdad exacta; faltan el fallo parcial seguido de reintento, las recreaciones durante una operación pendiente, canje/regeneración, canje/desvinculación, fallo de borrado y error de integridad no duplicado durante canje con conservación del código. El pool SQLite de una conexión y una barrera de salida no fuerzan esas intercalaciones internas. |
| 13 | resuelto | `link/CodeGenerator.java` y `link/AttemptTracker.java` documentan tamaño del espacio, probabilidades individual y distribuida, crecimiento con códigos vivos y necesidad de aprobación del arquitecto para una defensa agregada. No se añadió un requisito nuevo ni se presenta el límite individual como protección global. |
| 14 | resuelto a medias | `link/CodeGeneratorTest.java:78` sustituyó 1000 códigos únicos por al menos 995: reduce enormemente la probabilidad del falso fallo, pero sigue siendo una condición probabilística. `constructorAceptaSecureRandomPersonalizado` sigue usando azar real y solo comprueba longitud, por lo que no demuestra que se use el generador inyectado. Falta sustituir la prueba de diversidad por una fuente controlada y verificar de forma determinista el tratamiento de colisiones en persistencia, como pedía el hallazgo. |

Cambio fuera de zona (`mayorRoleId`): autorizado por el arquitecto, pero técnicamente incompleto por el vacío silencioso del método `default` y la búsqueda por nombre descritos en el 3. La llamada actual sale del executor de `unlink`, fuera del hilo principal, y consulta la caché del guild; no introduce una mutación fuera de la cola. El defecto es de identidad y tratamiento de ausencia, no un acceso a Towny desde ese hilo.

Veredicto final: requiere correcciones. Faltan cerrar los éxitos sin dependencias necesarias y sin retirada completa (2–3), identificar y reconocer el rol de alcalde por un ID estable (3), impedir el éxito sin sincronización y completar la recuperación del nombre y visibilidad de errores (5), eliminar la respuesta directa desde `defaultScheduler` (8), proteger la versión autorizada y coordinar las revocaciones frente a recreaciones con borrado condicional (9), y completar las pruebas observables y deterministas descritas en 12 y 14.

## Cierre final

Revisión estática limitada a esta ronda, sin ejecutar git ni Gradle. Build verde con 238 tests comunicado por el arquitecto; no es una ejecución propia. Las referencias conservan las raíces del informe.

| # | Estado |
|---|---|
| 2 | resuelto a medias |
| 3 | resuelto a medias |
| 5 | resuelto |
| 8 | resuelto |
| 9 | resuelto |
| 12 | resuelto |
| 14 | resuelto a medias |

El 9 queda resuelto en el alcance acordado: el adaptador transmite `linkedAt`, el servicio comprueba la versión antes de revocar y el `DELETE` condiciona UUID, Discord ID y fecha. No se afirma atomicidad entre Discord y almacenamiento. El 12 cuenta exactamente tres entradas reales, dos resultados inválidos y ocho bloqueos, y exige igualdad de la lista de roles. El 14 conserva el umbral y la prueba del constructor como deuda aceptada, sin bloquear; la igualdad de roles corresponde al 12. Los demás límites excluidos por el arquitecto no se reabren.

**1. ¿Está cerrada la identidad del rol de alcalde?** No del todo: con ID guardado, pasarela y ejecutor lo usan correctamente; `doConnect` transmite el mismo `settings`, pero ambos conservan constructores sin él y admiten nulo (`JdaDiscordGateway.java:55`, `:62`, `:126`; `JdaGuildOperationExecutor.java:49`, `:58`, `:429`). No hay cableado productivo en el punto de entrada que garantice esa dependencia: sin ella, un renombrado vuelve a ocultar el rol.
Con ajustes vacíos, `mayorRoleId` adopta y persiste el primer homónimo; sin coincidencia devuelve vacío, lo que no acredita ausencia de un rol previamente renombrado. Las tres pruebas de renombrado sí demuestran sus casos locales: resolución por ID, solicitud de retirada por ID y omisión sin ID con resultado exitoso; ninguna acredita inicialización y persistencia de extremo a extremo (`JdaDiscordGatewayTest.java:105`; `JdaGuildOperationExecutorTest.java:193`, `:230`).

**2. ¿Queda algún éxito sin hacer el trabajo?** Sí, por el 3: sin identidad persistida, la pasarela puede omitir al alcalde y `unlink` borrar el vínculo; incluso recibiendo su ID, el ejecutor puede omitirlo y devolver éxito (`JdaDiscordGateway.java:239`; `JdaGuildOperationExecutor.java:410`, `:415`). Por eso el 2 sigue parcial, aunque ya exige gateway y repositorio, propaga sus fallos y comprueba el resultado antes del borrado.
El 5 sí está cerrado en lo acordado: `syncService` es obligatorio y `SUCCESS` depende de completar su futuro. El 8 ya no ejecuta directamente la respuesta si falla el scheduler. No encontré otro éxito indebido independiente en los cambios revisados.

**3. ¿Introdujo alguna regresión?** Sí, en la inicialización de identidad de `discord/`: `verifyPermissions` persiste sucesivamente todos los homónimos y deja el último (`JdaDiscordGateway.java:216`), mientras `mayorRoleId` y `ensureMayorRole` eligen el primero sin ID previo. La comprobación de permisos ahora decide una identidad distinta según qué camino se ejecute primero y puede fijar un rol ajeno.
Además, las nuevas escrituras de `isManagedRole` y `ensureMayorRole` capturan el fallo de persistencia y continúan (`JdaGuildOperationExecutor.java:438`, `:513`): se puede conceder el rol y confirmar la operación sin guardar cómo reconocerlo tras renombrarlo. **Bloquea** cerrar esa identidad: exigir `settings`, resolver el arranque de forma coherente sin adoptar silenciosamente un homónimo ambiguo y propagar el fallo al guardar antes de conceder; cubrir esos caminos con pruebas directas. La deuda aceptada de pruebas no bloquea.

Veredicto final: requiere correcciones
