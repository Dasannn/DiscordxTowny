# T27 — first review

Scope: `main..149fbac`. Verdict: **blocked**.

- **F1 — blocking — `src/main/java/com/discordtowny/discord/JdaGuildOperationExecutor.java:349, 362-367, 1037-1040`.** `UNKNOWN_CHANNEL` from `setParent(archive)` can mean that the **destination archive category** disappeared between `ensureCategoryWithCapacity` and the move. The catch assumes the source text channel disappeared, skips its permission changes, and still saves `ARCHIVED` at line 399. The voice path at lines 370-388 has the same defect. The outer `classifyError` also turns any uncaught `UNKNOWN_CHANNEL` or `UNKNOWN_ROLE` in `ArchiveSpace` into success, even if the executor has not reached the state save. `DefaultSpaceService.archive` then sets `ARCHIVED` on that returned success. Correct behavior: treat absence as completed work only after identifying the missing resource as the one the archive intended to remove or move; a missing destination or an incomplete archive must fail and remain visible for repair. Keep `CreateSpace`, `RenameSpace`, `RestoreSpace`, and `ApplyMemberRoles` strict. Add a check for a destination-category race and for an outer-classifier error before the archive state save.
- **F2 — blocking — `src/main/java/com/discordtowny/space/DefaultSpaceService.java:288-293` (outcome created at `src/main/java/com/discordtowny/discord/JdaGuildOperationExecutor.java:405-407`).** The success note naming already missing resources is discarded. The audit row records only the Towny reason, so the Discord log channel, which renders audit events, also shows only that reason. `DefaultSyncService.executeArchive` reduces the result to a boolean, so `/dt sync` reports a repair count but no missing-resource note. Only the server logger at executor line 406 has the detail. The owner watching the Discord log channel never learns what was already gone, and `/dt admin info` cannot retrieve it from audit history. Correct behavior: carry the note into the successful archive audit detail and its Discord rendering, and expose it in the sync report where an archive is reported, once per completed archive.

## Checks without findings

- The normal loop closes: the executor saves `ARCHIVED` at line 399, `DefaultSpaceService.archive` updates the state at line 289, and the next `reconcileAll` pass returns at `DefaultSyncService.java:454-455` for a deleted Towny town or at lines 499-501 for a ruined town. It submits no further archive. The two new early returns also suppress `sync.problem-town-no-longer-exists` in report mode. An operator can no longer see *from `/dt sync`* that an already archived town has since disappeared from Towny; that archived/deleted pairing is expected, so this does not hide a repairable inconsistency. Towny read failures still produce problems before those returns.
- `execute` routes all six operation types through `classifyError`. The changed branch tests the concrete operation type, so `CreateSpace`, `RenameSpace`, `RestoreSpace`, and `ApplyMemberRoles` retain permanent failure for `UNKNOWN_CHANNEL` and `UNKNOWN_ROLE`; `DeleteSpace` already had success semantics. The corrected create guard test reaches `textChAction.complete()`, which throws `UNKNOWN_CHANNEL`, and then checks permanent failure. It does not cover F1.
- The local archive catches rethrow permission, access, rate limit, and capacity errors; they swallow only `UNKNOWN_ROLE` on role deletion and `UNKNOWN_CHANNEL` during channel moves or permission updates. The destination ambiguity is F1. Counting surviving channels before `ensureCategoryWithCapacity` is sound: with none left, there is no channel to move and no reason to create an empty archive category; the role can still be removed.
- Purge's confirmed path reads `findAll` for an inconsistent count, then `purgeArchived` independently reads `findByState(ARCHIVED)`. Concurrent reconciliation can change the skipped count between those reads, so it is a snapshot, while the deleted count is the actual completed purge count. The reported skipped count is advisory and never authorizes deletion; this bounded race does not justify blocking. The command never purges inconsistent spaces. Both catalogs have the new message and matching `{count}` placeholder; the command tests cover only-inconsistent, mixed, and console responses.

Verification: code and read-only diff review only. I did not run Gradle or tests; the architect reports the suite green on this tip. I made no code changes.
